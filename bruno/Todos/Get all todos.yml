info:
  name: Get all todos
  type: http

http:
  method: GET
  url: "{{baseUrl}}/api/{{version}}/todos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​